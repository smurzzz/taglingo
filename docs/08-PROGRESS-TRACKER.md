# TagLingo — Progress Tracker

Living status doc. Update this after every work session — it should always reflect reality. Status values: **Not started** · **In progress** · **Blocked** · **Done**.

---

## Phase Status

| Phase | Exit Criterion (from `01-PHASE-PLAN.md`) | Status | Notes |
|---|---|---|---|
| 0 — Environment & Installation | Empty app runs, connects to Supabase, connects to Clerk | In progress | Scaffold, deps, env wiring done (commit `fbed274`); lint/typecheck/expo-doctor 21/21/Android export all clean. Blocked on: user creates Supabase + Clerk projects and pastes real keys into `.env`; then verify live connections via Expo Go. |
| 1 — All UI Screens (static) | Reviewer can tap through every screen using only mock data | Done | All 11 screens + offline state built and navigable via mock data (18 words across 3 levels, seeded progress). Verified `expo lint`, `tsc --noEmit`, `expo-doctor` 21/21 clean. Mock-applicable critical-path tests pass (see below). |
| 2 — Data Layer, Schema & Word Seeding | `words` table live and seeded from CSV, RLS verified | Done | Migrations authored (`supabase/migrations/20260925000000_schema.sql` + `20260925000001_seed_words.sql`), applied to live project `wvquienibojiphxamgnr` via the Supabase Management API, and recorded in `supabase_migrations.schema_migrations` so `npx supabase db push` won't re-apply. Verified live: seed split Beginner 10 / Intermediate 8 / Advanced 5; all 5 tables RLS-enabled; CP-01 + CP-02 pass end-to-end via PostgREST (throwaway auth user, cleaned up); anon is fail-closed. `src/types/database.ts` regenerated to match. Lint/typecheck/expo-doctor clean. |
| 3 — Auth & Roles | Real account logs in, lands on Home Dashboard, Profile reflects real data | In progress | Code + DB live: migration `20260926000000_clerk_auth.sql` applied (users.id → text = Clerk `sub`, RLS via `auth.jwt()->>'sub'`, `ensure_user` RPC) and registered in `supabase_migrations.schema_migrations`; `src/types/database.ts` regenerated. New `src/lib/auth.tsx` + Clerk-bound `src/lib/supabase.ts` + `src/features/user/api.ts`; real Clerk login (`useSSO`, email-code); Profile/Settings wired; lint / tsc / expo-doctor clean. **Integration verified live:** Clerk↔Supabase Third Party Auth entry present via Management API (issuer `https://optimal-halibut-3418.clerk.accounts.dev`, type `clerk-development`, JWKS resolved); Clerk "Connect with Supabase" done. Only the final on-device login remains. |
| 4 — Flashcard Study & Progress | Mastered word reflects instantly across Home/Browse/Progress | In progress | Code + DB done: migration `20260927000000_progress_word_status.sql` applied live (`'new'` enum value + `status` default) and registered in `supabase_migrations.schema_migrations`; `src/features/words/api.ts` + `src/features/progress/api.ts` read `words`/`word_progress`/`study_sessions` through the Clerk-bound client with a mock fallback; mutations (grade / favorite lifecycle / daily study session) upsert via RLS (simulated JWT claims verified live — CP-11/CP-12) and invalidate the progress/words keys. Study/Home/Browse/Progress/Profile rewired off the mock store; weekly history relabelled words, not minutes. Lint / tsc / expo-doctor clean. Remaining: on-device login (same as Phase 3) to see real rows and close the exit criterion. |
| 5 — Definition Lookup & Quiz Mode | Full quiz runs end to end, missed word reviewable from results | In progress | Code + DB done: `useDefinition` calls the Free Dictionary API live (graceful `{ found: false }` on 404/network failure — holds even while the API itself was returning 522 during verification); `buildQuizFromDeck` builds up to 10 questions from real per-level words with same-level distractors; `quiz.tsx` records an attempt via new `src/features/quiz/api.ts` `useRecordQuizAttempt` plus the daily `study_sessions` touch; "Review these" passes the missed-word IDs and Study filters to them. `quiz_attempts` insert verified live under simulated JWT claims (CP-16). Lint / tsc / expo-doctor clean. Remaining: on-device run (same login blocker as Phases 3/4) to confirm the full quiz + definition flow end to end. |
| 6 — Notifications & Offline State | Reminder fires; offline state shows correctly | In progress | Code done, device pending. **Expo Go caveat resolved 2026-09-27 (post-6.5 fix):** `expo-notifications` was removed from Expo Go in SDK 53 and *importing* it throws at module scope, crashing the whole app in Expo Go. `src/features/notifications/api.ts` now lazy-loads the module behind an `expo-constants` execution-environment check (`StoreClient`, i.e. Expo Go, → every function is a safe no-op); `_layout.tsx` deep-links via `addNotificationResponseReceivedListener` instead of the hook. Reminders therefore need a development build (`npx expo run:android` / `eas build --profile development`) — Expo Go runs fine without them. `src/features/notifications/api.ts` (DAILY trigger, fixed id, Android channel, PROVISIONAL-aware permission helpers); `<ReminderSync/>` + response listener mounted in `_layout.tsx`; Settings requests permission on enable and shows a denied hint (copy updated to explain the Expo Go/dev-build split); React Query `onlineManager` bridged to NetInfo for auto-refetch on reconnect. Vocabulary expanded 23→165 (Beginner 68 / Intermediate 61 / Advanced 36) via idempotent migration `20260927000001_expand_vocabulary.sql` (applied + registered live, CP-17). Lint / tsc / expo-doctor 21/21 clean. Remaining: on-device confirmation (reminder fires, network-cut recovery) — now requires a dev build instead of Expo Go. |
| 6.5 — Functionality Spec Compliance (§1–§11) | Every screen matches the `07-FUNCTIONALITY-PROMPT.md` spec; all Open Decisions resolved | Done (code) | Audited all 11 screens against §1–§11 and closed every gap: login gates on a valid email + Terms/Privacy open in the external browser (decision, §1); Home greeting icon by time of day, notifications bell → in-app dropdown of recent alerts (streak + reminder, derivation — decision, §2), "Daily goal" and "Continue studying" both resume the last-studied level; Study grades/favorites disabled while in flight, offline grades fail cleanly with an inline notice, back-on-close with fallback (§4); DefinitionSheet gained a drag-handle affordance and a real `status` badge prop (§5); Quiz close-confirm discards the attempt without writing `quiz_attempts` (§6); Quiz Results missed rows are read-only (§7); Word Progress added a debounced (300ms) search, newest-updated-first sorting, and row-tap → read-only Word Detail reusing the §5 sheet — decision, §8); Profile already matched the spec (weekly chart = words metric, decision §9); Settings replaced the ±steppers with the native time picker (`@react-native-community/datetimepicker`), theme previews preview with revert-on-leave (decision), logout clears the React Query cache (§10); OfflineBanner "Try again" does a real `NetInfo.fetch()` (§11). Fixed three Cebuano typos from the Phase 6 expansion live via migration `20260928000000_fix_vocabulary_spelling.sql` (baguised→bugnaw, binulan→bulan, lungod→lungsod; applied + registered, CP-19). Lint / tsc / expo-doctor 21/21 clean. Remaining: on-device eyeball of the new UI (gated on the same login as Phases 3–6). |
| 7 — Polish, Test, Submit | Installable APK, demo runs start to finish, word count target met | Not started | |

## Critical-Path Test Status (mirrors `05-TESTING-REPORT.md`)

| Test | Status |
|---|---|
| CP-01 | ✅ verified live on Supabase (postgREST, auth user) — cross-user `word_progress` write → 403, read → own rows only |
| CP-02 | ✅ verified live on Supabase — authenticated `POST /words` → 403; anonymous → 401 |
| CP-03 | ✅ verified against mock quiz generator (distractors always same-level) |
| CP-04 | ✅ verified against mock state + React Query invalidation on grade/favorite |
| CP-05 | ✅ verified — `recordQuiz` writes `lastQuiz` only (plus the `quiz_attempts` row from Phase 5, CP-16), never touches `status` |
| CP-06 | ✅ verified — DefinitionSheet renders "No definition available" on `{found:false}`; Phase 5's live `useDefinition` maps the Free Dictionary API onto the same graceful branch |
| CP-07 | ✅ verified — root connectivity hook + full-screen Offline state + demo switch |
| CP-14 | ✅ verified (Phase 5) — definition lookup fetches the live API, renders part of speech/definition/example, and degrades to the graceful miss on 404/network failure |
| CP-15 | ✅ verified (Phase 5) — quiz questions + same-level distractors built from real per-level words; results ring/missed list resolve real word ids |
| CP-16 | ✅ verified live (Phase 5) — one `quiz_attempts` row per completion, isolated per user, RLS blocks forging, never auto-grades `word_progress` |
| CP-17 | ✅ verified live (Phase 6) — generator emits exactly the 142-word delta; expansion migration applied + registered, live counts Beginner 68 / Intermediate 61 / Advanced 36 |
| CP-18 | 🔶 code verified (Phase 6) — reminder scheduling + permission flow + NetInfo→`onlineManager` reconnect; on-device fire/network-cut pending CP-10 login |
| CP-19 | ✅ verified live (Phase 6.5) — `20260928000000_fix_vocabulary_spelling.sql` corrected three Phase 6 translation typos (cold/month/city) with UPDATEs keyed on tagalog+english (no duplicate natural keys), registered in `schema_migrations`, live row values confirmed |
| CP-20 | ✅ verified (Phase 6.5) — Google/Apple sign-in no longer lands on Expo Router's "Page could not be found": Clerk's `startSSOFlow` redirects to `AuthSession.makeRedirectUri({ path: 'sso-callback' })`, and `src/app/sso-callback.tsx` (+ Stack registration) absorbs that deep link; warm-path session completes via `setActive`, AppGate then routes to Home, or to /login if no session arrives |
| PS-01 | ✅ verified (Phase 4) — streak computed from `study_sessions` (consecutive days ending today/yesterday), weekly counts from `word_progress.updated_at` |
| PS-02 | ✅ verified — completion % computed directly from `state.status` |
| PS-03 | ✅ verified — favorites tracked independently of status |

## Vocabulary Seeding Progress
Track expansion from the CSV template (23 starter words) toward the 200–300 word target (`01-PHASE-PLAN.md` Phase 6). Phase 6 took 23 → 165 words; remaining growth and a human translation-accuracy pass are deferred to Phase 7.

| Level | Current Count (live) | Target |
|---|---|---|
| Beginner | 68 | ~70-100 |
| Intermediate | 61 | ~70-100 |
| Advanced | 36 | ~60-100 |

## Known Blockers
- **No local Supabase verification path** — Docker is not installed on this machine, so `supabase start`/`db lint`/locally-restarting PostgREST can't run. Phase 2/3 migrations were applied and verified live via the Supabase Management API instead. DB-level `npx supabase link` + `supabase db push` can't run without the Postgres password; prefer the Management API (`POST /v1/projects/<ref>/database/query`) for any future DDL.
- **Clerk↔Supabase Third Party Auth** is now active — confirmed live via Management API `GET /config/auth/third-party-auth` (issuer `https://optimal-halibut-3418.clerk.accounts.dev`, type `clerk-development`, JWKS resolved). The Clerk instance is a **development** instance: correct for Expo Go testing, but the production Clerk instance needs its own Connect + TPA entry before release.
- **Security: rotate the Supabase access token** — an earlier `.env.example` contained the real token (`sbp_df8a…`); it was redacted before commit but remains in local git history. Rotate it in Supabase → Account → Access Tokens and update `.env`.

## Open Decisions
- [x] Daily goal word count on Home Dashboard — hardcoded at 20 for v1; revisit if it should be user-configurable. *(revisit deferred to Phase 7, not changed)*
- [x] Word Progress row tap — resolved in Phase 6.5: opens the read-only Word Detail (DefinitionSheet with status + favorite), never a study session (§8).
- [x] Notification bell on Home — resolved in Phase 6.5: lightweight in-app dropdown of recent alerts derived from streak + reminder state (no history table in the schema) (§2).
- [x] Terms of Use / Privacy Policy — resolved in Phase 6.5: open in the external system browser via `expo-web-browser`; URLs are placeholders (`https://taglingo.app/terms`, `https://taglingo.app/privacy`) until the marketing site ships (§1).
- [x] Offline-time mutations — resolved in Phase 6.5: grades/favorites clearly fail with a small inline notice (Study) rather than queueing; nothing is silently dropped or half-recorded (§4/§11).
- **2026-09-27 (OAuth unmatched-route fix):** first on-device Google/Apple sign-in landed on Expo Router's "Unmatched Route / page could not be found" — Clerk `useSSO.startSSOFlow` redirects OAuth back to `AuthSession.makeRedirectUri({ path: 'sso-callback' })` and no such route existed. Added `src/app/sso-callback.tsx` (branded spinner + "Finishing sign-in…", warm-path `setActive` completes the session and AppGate routes to Home; no session → AppGate bounces to /login) and registered `<Stack.Screen name="sso-callback" />`. CP-20 verified by code; tsc / lint / expo-doctor 21/21 clean.
- [ ] Human review pass over the Phase 6 vocabulary expansion (142 new Tagalog/Cebuano pairs) for translation accuracy before the 200–300 target is finalized in Phase 7. Phase 6.5 already corrected three live typos (baguised, binulan, lungod) — the full manual sweep still stands.

---

**Last updated:** 2026-09-27 (Phase 6.5 — functionality spec §1–§11 compliance pass. Login gates on a valid email, Terms/Privacy open in the external browser; Home greeting icon tracks time of day, the bell opens an in-app alerts dropdown and both study targets resume the last-studied level; Study pauses in-flight grades, fails offline mutations with an inline notice, and closes via back-with-fallback; DefinitionSheet shows a drag-handle affordance and a real status badge; Quiz confirms before discarding an attempt (§6) and Results' missed rows are read-only; Word Progress gained debounced search + newest-first order + a read-only Word Detail; Settings uses the native time picker, previews-without-commit for themes, and a cache-clearing logout; OfflineBanner's Try again does a real `NetInfo.fetch()`. Live data: three Phase 6 translation typos corrected by migration `20260928000000_fix_vocabulary_spelling.sql` (applied + registered on `wvquienibojiphxamgnr`, live values confirmed). Lint / tsc / expo-doctor 21/21 clean. On-device eyeball of Phases 3–6.5 output is still gated on the CP-10 login.)